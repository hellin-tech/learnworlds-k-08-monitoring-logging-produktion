apiVersion: v1
kind: Service
metadata:
  name: novatech-api
  namespace: novatech-dev
  labels:
    app: novatech-api
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: novatech-api
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
